Console Command:

sudo -H gedit /etc/environment
SPRING_MAIL_USERNAME="your user name"
SPRING_MAIL_PASSWORD="your password"

APPLICATION.PROPERTIES

spring.mail.username=${spring.mail.username} (this will fetch user name from the environment variable)
spring.mail.password=${spring.mail.password} (this will fetch password from the environment variable)